<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <groupId>com.getaxonflow.examples</groupId>
    <artifactId>spring-boot-example</artifactId>
    <version>1.6.0</version>
    <packaging>jar</packaging>

    <name>AxonFlow Spring Boot Example</name>
    <description>Spring Boot integration with AxonFlow AI Governance</description>

    <properties>
        <java.version>17</java.version>
        <!-- Override Tomcat version to fix CVE-2026-29145, CVE-2026-29129, CVE-2026-34483,
             CVE-2026-34487 and, from #3116, CVE-2026-41293 / CVE-2026-43512 / CVE-2026-43515
             (CRITICAL) plus CVE-2026-41284 / CVE-2026-42498 / CVE-2026-43513 (HIGH), and
             CVE-2026-65182 (CRITICAL) / CVE-2026-65905 / CVE-2026-68525 fixed in 10.1.58,
             which Maven Central never published; 10.1.59 is the first release above it -->
        <tomcat.version>10.1.59</tomcat.version>
        <!-- Override Spring Framework to fix CVE-2025-41249 and, from the
             #3038 security scan, CVE-2026-41850 (spring-expression DoS via
             crafted SpEL expressions), CVE-2026-41842 (spring-webmvc DoS
             resolving static resources) and CVE-2026-41845 (spring-webmvc
             XSS via incorrect JavaScript escaping) -->
        <spring-framework.version>6.2.19</spring-framework.version>
        <!-- Override Jackson to fix GHSA-72hv-8253-57qq -->
        <jackson-bom.version>2.22.1</jackson-bom.version>
        <!-- Override Micrometer to fix CVE-2026-40983 / CVE-2026-40984
             (micrometer-core Denial of Service; pulled transitively via
             spring-boot-starter-actuator), published 2026-08-07 -->
        <micrometer.version>1.15.12</micrometer.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>2.22.1</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>2.22.1</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Override transitive jackson-databind for CVE fix (CVE-2026-54512, CVE-2026-54513) -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.22.1</version>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- AxonFlow SDK -->
        <dependency>
            <groupId>com.getaxonflow</groupId>
            <artifactId>axonflow-sdk</artifactId>
            <version>9.2.0</version>
        </dependency>

        <!-- OpenAI for Gateway Mode demo -->
        <dependency>
            <groupId>com.theokanning.openai-gpt3-java</groupId>
            <artifactId>service</artifactId>
            <version>0.18.2</version>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
