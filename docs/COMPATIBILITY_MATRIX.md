# SDK-Platform Compatibility Matrix

This document maps platform versions to minimum SDK versions and the features each release introduced.

**Current (platform 10.2.0):** minimum SDK **8.0.0** (Go/Python/TypeScript/Java; Rust **0.7.0**), recommended SDK **9.1.1** for Go and **9.1.0** for Python/TypeScript/Java (Rust **0.8.2**). These floors are what the platform `/health` endpoint advertises (`min_sdk_version` / `recommended_sdk_version`). Neither 10.1.0 nor 10.2.0 changed either map.

**The 10.0.0 platform major does NOT move the SDK floor.** The Go, Python, TypeScript and Java floor stays at 8.0.0, as it has continuously since v7.9.0 and across the v8.0.0 and v9.0.0 platform majors before it. The Rust floor stays at 0.7.0, which is the only value it has ever held: Rust was absent from `MinSDKVersion` at v7.9.0, v8.0.0, v9.0.0 and v9.6.1, and joined the compatibility maps at v9.7.0 (see the v9.7.0 row below). A platform major and an SDK floor are independent axes on this platform: the floor moves only when the platform stops speaking a wire contract an older SDK depends on, which 10.0.0 does not do.

> **Note on version numbers:** SDK versions are deliberately decoupled from platform versions. The Go SDK's module path tracks its major: SDK 8.x is `github.com/getaxonflow/axonflow-sdk-go/v8`, SDK 9.x is `.../v9`. The Rust SDK is on an independent 0.x line (currently 0.8.2) and does not carry the 9.0.0 LangGraph/computer-use adapters.

## Version Compatibility

| Platform Version | Min SDK Version | Recommended SDK | Key Features Added |
|-----------------|----------------|-----------------|-------------------|
| v10.2.0 | v8.0.0 (Rust v0.7.0) | v9.1.1 Go, v9.1.0 Python/TypeScript/Java (Rust v0.8.2) | **Additive and dark; no floor change, and with the new switch unset and no per-organization record no status or verdict change on any existing endpoint.** **One additive migration, `enterprise/146`, Enterprise only** (a community deployment applies nothing): it CREATEs `identity_org_settings`, one row per organization, and ALTERs no existing table, so there is no backfill, no data mutation and no lock on an existing relation; RLS is ENABLEd and FORCEd with an isolation policy on `app.current_org_id`. It creates no rows, and an organization with no row resolves to the deployment's process flag exactly as on 10.1.0; an Enterprise deployment that has not applied it yet also keeps 10.1.0 behaviour, because the settings read fails and falls back to the process mode. The ADR-065 policy decision and identity control plane lands as infrastructure that no request path consults: a separate `platform/decision` Go module (canonical authorization contract with a strict AuthZEN 1.0 adapter, in-process deterministic OPA policy decision point over signed digest-pinned bundles, typed policy authoring, action/tool/resource/enforcement-point registries, the executable conformance corpus with its 47-row source-case disposition ledger, and the legacy-policy compiler plus shadow semantic-diff gate), the identity plane primitives in `platform/shared/identity` (trust realms, realm-qualified principals, actor chains, directory graph), and the stateful requirements plane in `platform/shared/requirements` (typed obligations, compound approval clauses, signed decision proofs, atomic reservations, detector assurance). The only thing that touches a request path is the compatibility adapter's entry call, which returns immediately unless one new switch selects a mode: **`AXONFLOW_IDENTITY_COMPAT_MODE`** (`off` by default; `shadow` records what the identity plane would have decided while legacy authentication continues to decide every request; `enforce` refuses a credential the identity plane does not admit, narrowable with `AXONFLOW_IDENTITY_COMPAT_ENFORCE_REASONS`). An unrecognized value of either variable is a BOOT FATAL, so a deployment that sets it must spell it correctly; unset is unchanged behavior. **On Enterprise the same mode is also settable per organization**, and a record wins over the process flag in both directions, through three new platform-operator routes behind the existing admin API key: **`GET`, `PUT` and `DELETE /api/v1/admin/organizations/{org_id}/identity-settings`** (`GET` answers `404` when no row exists, which is the ordinary state; `DELETE` is idempotent). A write reaches the agent and the orchestrator only after their memo window expires, bounded by the new `AXONFLOW_IDENTITY_ORG_SETTINGS_TTL_SECONDS` (default 60, clamped 1-600, never fatal); there is no invalidation channel. **One new endpoint is added to the agent, and it is Enterprise-only and opt-in**: **`POST /api/v1/identity/caep/events`**, an authenticated OpenID Shared Signals (CAEP) push receiver registered only in an enterprise build with its collaborators wired, taking the organization from the caller's credential and never from the event body, resolving the issuer against the realm registry and checking the algorithm before any key fetch, and refusing everything for an organization whose `identity_org_settings` row has not opted in with an audience. Its refusals carry the RFC 8935 error body, with `400`/`403` meaning stop and do not redeliver and `503` meaning redeliver; two Prometheus counters are exported on the agent's `/prometheus`. No existing endpoint gains or loses a status. See `docs/security/identity-compat-mode.md`. One customer-visible fix independent of the switch: licence `days_until_expiry` is now counted in whole days between UTC dates on every validator and both licence generators format their date in UTC, including the plugin-claim generators' zero-`IssuedAt` fallback, so an existing licence's days-remaining number moves by at most one day in the widening direction and a 30-day licence minted after the upgrade reports 30 where the old pipeline could report 28. `axonctl` now sends `X-Client-ID` alongside the deprecated `X-Tenant-ID`. The existing check/decide endpoints keep their status, verdict and wire shapes (Enterprise only, and only with the identity plane running: the fleet-validator and portal-SSO `401` outage errors are reclassified so a revocation store or JWKS that cannot be consulted says so instead of reading as an invalid token; with the mode unset every error body is byte-identical to 10.1.0, and community binaries are unchanged); the AuthZEN-native wire surface and the per-plane PDP shadow mode named in the ADR-065 release plan for this minor are NOT in this release (see the CHANGELOG's operator notes). SDK recommendations unchanged |
| v10.1.0 | v8.0.0 (Rust v0.7.0) | v9.1.1 Go, v9.1.0 Python/TypeScript/Java (Rust v0.8.2) | The US compliance framework family (epic #3528): three new compliance-report regulators (`usinsurance`, `usbanking`, `ussecurities`; `framework` REQUIRED on each, as for `ojk`), nine US policy templates across `compliance-glba`, `compliance-fairlending`, `compliance-bsa-aml` and `compliance-nydfs` plus regulator-keyed retention presets, the `jsonl` artifact format (offered to `ussecurities` alone), and five portal read routes (`GET /api/v1/usinsurance/{inventory,oversight,data-sources}`, `GET /api/v1/usbanking/exam-readiness`, `GET /api/v1/ussecurities/exam-readiness`). Five additive migrations (`core/168`; `enterprise/139`, `142`, `144`, `145`), none taking a long lock; a community deployment applies only `core/168`. **One authorization change an integrator can hit:** the three audit-chain verification routes (`GET /api/v1/audit/chains/{id}/verify`, `/records/{id}/verify`, `/signing-key`) now require compliance read authority (`admin`, `owner` or `policy_admin`, or an asserted internal-service identity) and answer `403` to any other authenticated member; community mode is exempt, the hosted Community SaaS is not. Four builtin templates with a list-valued variable that substituted to a nested array (two `in` templates that never fired, two `not_in` templates that fired on every role) are corrected at source and forward-fixed by `enterprise/139`, with `core/168` repairing the community Content Safety Filter row. No floor change |
| v10.0.0 | v8.0.0 (Rust v0.7.0) | v9.1.1 Go, v9.1.0 Python/TypeScript/Java (Rust v0.8.2) | **Breaking, no floor change.** **The largest break is org-scoped policy selection, and it is a schema change plus a deploy-ordering requirement.** A policy row is now selected by the organisation id carried in the signed licence, not by `tenant_id` (the Basic-auth username, which the caller chooses and nothing validates). For one tenant per organisation this is a no-op; where one organisation owns several tenants holding different rows, those rows begin applying organisation-wide. `organization_id` is DROPPED from `static_policies`, `dynamic_policies` and `policy_overrides`, so any SQL reading it against those tables breaks. `org_id` becomes `NOT NULL` and non-empty on the same three tables; a row for which no organisation can be resolved is stamped `__axonflow_unowned__` and **stops being selectable by anyone**, which is the one change in the release that removes enforcement rather than widening it. Run `./preflight.sh` checks 23 and 24 to name the affected rows BEFORE upgrading. Seven orchestrator policy-authoring routes now answer `401 ORG_REQUIRED` without an `X-Org-ID` header, including `POST /api/v1/templates/{id}/apply`, which previously answered `201`; these are not proxied by the agent, so nothing sets the header for you. **Deploy ordering: roll the agent and the orchestrator together, and never the orchestrator first** (the agent now sends the organisation id on its internal policy-evaluation call, a release orchestrator answers `403` without one, and the agent refuses to absorb a `403`, so an orchestrator-first skew window denies every governed MCP tool call). **Governance segments are enforced on five further planes**, not three: the gateway pre-check, the WCP HTTP step-gate, the MCP-server `check_policy`/`check_output` tools, the four MCP REST routes (`/mcp/resources/query`, `/mcp/tools/execute`, `/api/v1/mcp/check-input`, `/api/v1/mcp/check-output`) and `POST /api/v1/decide`. Each denies fail-closed when the segment set cannot be resolved. **On the MCP REST routes, `/decide` and the WCP step-gate, enforcement is CONDITIONAL on the caller presenting a per-user token**: a token-less caller is still served organisation-only with no refusal, so do not drop an edge gate on the strength of this row unless the organisation also sets `require_user_token`. `POST /v1/chat/completions` still evaluates organisation-only and is not covered. **`X-User-Token` becomes required on the two MCP-server tools** for any organisation holding an enabled segment-scoped policy for that phase, and a per-user token that FAILS to validate is now `401` on the four MCP REST routes instead of being downgraded to a synthetic service identity (not opt-in). **New `require_user_token`** (per-organisation column plus `AXONFLOW_REQUIRE_USER_TOKEN`, default off) refuses a token-less enterprise caller at authentication on six gate points; an unparseable env value is a BOOT FATAL, and the WCP step-gate has no gate point for it. **The SAML SP signing keypair is no longer fabricated** when Secrets Manager cannot be read: a tenant that does not store a complete keypair of its own is refused at configuration-load time, on either half being missing, rather than being served a keypair that changed on every restart. Wire-shape changes an integrator must handle: the six the "Old SDK + New Platform" row below enumerates, plus three more. The six: `avg_latency_ms` becomes nullable on the audit report/summary and session-summary; `tokens_used`, `cost` and `response_time_ms` are OMITTED rather than sent as `0`; the LEGACY `POST /api/v1/sebi/audit/export` reports `status: "partial"` and omits `summary.compliance_score` on an incomplete export, while the `POST /api/v1/compliance/reports` facade's status vocabulary is unchanged and never returns `partial`; declarative-workflow run ids change prefix from `wf_` to `wfe_`, which breaks a client that prefix-matches them; `POST /api/v1/audit/report` returns `500` where it previously returned a short table; an unscoped `/api/v1/unified/executions` list is now `401`. The three additional: `POST /api/v1/mcp/evaluate-policies` gains a `segment_ids` array on the request body; `POST /api/v1/mcp/check-input` idempotency entries are scoped to the calling principal, so the stored `endpoint` value changes shape and keys cached before the upgrade no longer replay (operator queries must switch from equality to a prefix match); and audit attribution on `/api/v1/mcp/check-input` and `/api/v1/mcp/check-output` now names the validated per-user identity rather than a trusted `X-User-Email`. `axonflow_segment_resolution_total` also gains a `phase` label, which empties any exact-series dashboard selector. The in-memory `DynamicPolicyEngine` and `AXONFLOW_DEBUG_POLICIES` are removed. **EIGHT migrations (`core/160` through `core/167`), not three.** `161` and `162` are whole-table `audit_logs` backfills whose runtime scales with your row count; they take `ROW EXCLUSIVE` only, so readers are never blocked. `165`, `166` and `167` are cheap but hold `ACCESS EXCLUSIVE` to `COMMIT` and therefore **block READS**: `165` and `166` on all three policy tables together, `167` on `hitl_approval_queue` and `hitl_approval_history` (the portal Approvals page) while it builds two indexes and validates a constraint. No migration in this release rewrites a table. See the CHANGELOG's Migration section for the per-migration lock and scan analysis |
| v9.19.0 | v8.0.0 (Rust v0.7.0) | v9.1.1 Go, v9.1.0 Python/TypeScript/Java (Rust v0.8.2) | Portal read surfaces (audit, approvals, policies) reconciled with what the platform recorded; the last four bespoke policy-condition matchers replaced by one shared evaluator. Policy rows stored with an explicitly-empty `conditions` array stop being evaluated by the database-backed engines. Go SDK recommendation moves to 9.1.1 and Rust to 0.8.2; no floor change |
| v9.18.0 | v8.0.0 (Rust v0.7.0) | v9.1.0 (Rust v0.8.1) | Fraud & Risk Add-on (Enterprise add-on, ADR-061): deterministic financial-crime policy evaluation on the decide and MCP planes plus an optional self-hosted ML risk scorer, and CloudFormation alarm/permission fixes. No migrations, no floor change |
| v9.17.0 | v8.0.0 (Rust v0.7.0) | v9.1.0 (Rust v0.8.1) | Self-service password recovery, forgot-password security hardening, segment-scoped policy targeting. No floor change |
| v9.16.1 | v8.0.0 (Rust v0.7.0) | v9.1.0 (Rust v0.8.1) | Compliance exports: policy attribution across all audit planes (the OJK report's Policy column was blank for every row on 9.16.0). Patch, no floor change |
| v9.16.0 | v8.0.0 (Rust v0.7.0) | v9.1.0 (Rust v0.8.1) | Interactive OIDC portal login, OIDC endpoint SSRF hardening. Enterprise-only train: the Community agent and orchestrator binaries are byte-identical to 9.15.0 and are released at 9.16.0 only so the two editions never diverge in version number. No floor change |
| v9.15.0 | v8.0.0 (Rust v0.7.0) | v9.1.0 (Rust v0.8.1) | In-vpc SAML SSO fixed end-to-end plus SSO role auto-provisioning on first login; the governed `/api/request` empty-email fail-close RELAXES to proceed org-only; telemetry digest breaks down by platform version. No migrations, no floor change |
| v9.14.1 | v8.0.0 (Rust v0.7.0) | v9.1.0 (Rust v0.8.1) | Patch: RBI audit export accepts PDF and XLSX, and enterprise-only source is removed from the community distribution. One additive migration, no data mutation, no floor change |
| v9.14.0 | v8.0.0 (Rust v0.7.0) | v9.1.0 (Rust v0.8.1) | Jurisdiction-selectable compliance reports with real PDF/CSV/XLSX renderers, Indonesia (OJK/BI/UU PDP) export data completeness, segment-scoped static policy targeting. SDK 9.1.0 minors add the real audit read-model wire fields (`policy_decision`, `policy_details`, `response_time_ms`, `action`) additively and deprecate `request_type`; no floor change |
| v9.12.0 - v9.13.0 | v8.0.0 (Rust v0.7.0) | v9.0.0 (Rust v0.8.1) | Five-role RBAC enforced (`owner`/`admin`/`policy_admin`/`developer`/`viewer`; `member` removed), owner bootstrap + lifecycle + break-glass, policy CRUD RBAC-gated on every family, permission-driven PII visibility, JumpCloud SSO preset, SCIM deprovision revokes roles fail-closed, `tokens` column on audit-export CSV. Static-policy enforcement restored on the least-privilege DB posture (9.12.2); authoritative principals, scoped pre-auth lookups and deployment-mode validation (9.13.0). No SDK floor change across the range. |
| v9.11.0 | v8.0.0 (Rust v0.7.0) | v9.0.0 (Rust v0.8.1) | Streaming prompt-DLP (ext_proc SSE legs), seam-capability-aware obligations + `obligation_fallback` posture, matched-PII policies never return a bare allow, Path B fleet-identity fixes (per-org fleet role seeding, SCIM org key), tool identity: `caller_name` + separate `tool` field on check-output. **SDK 9.0.0 majors are required for the de-concatenated `tool` wire field** (check-output floor: platform 9.11.0) |
| v9.10.0 | v8.0.0 (Rust v0.7.0) | v9.0.0 (Rust v0.8.1) | Per-user identity for fleets: `X-User-Token` (admin-minted HS256 + OIDC/JWKS "Path B"), role-scoped reads on audit/decisions/overrides/cost/replay. **Behavior change:** shared-credential callers with no per-user token read zero rows from governed read tools. Separate `tool` field accepted on check-input (floor: platform 9.10.0) |
| v9.9.0 | v8.0.0 (Rust v0.7.0) | v8.5.1 (Rust v0.8.1) | Trust-gated identity headers: `AXONFLOW_TRUST_IDENTITY_HEADERS` (default **off**) gates `X-User-Email`/`X-User-ID`/`X-Session-Id` attribution on all four governance planes (`identity_header_attribution` capability); session-override forgery closed on every override-apply ingress |
| v9.8.0 | v8.0.0 (Rust v0.7.0) | v8.5.1 (Rust v0.8.1) | agentgateway/Envoy PEP adapters (ExtMcp, ext_authz, ext_proc; adapters require platform ≥9.7.0), 41-column TIMESTAMPTZ retype migrations (plan a maintenance window on large `audit_logs`), API-reference reconciliation |
| v9.7.0 | v8.0.0 (Rust v0.7.0) | v8.5.1 (Rust v0.8.1) | Fail-closed hardening sweep (request plane, gateway, check-output `redaction_evaluated`), per-client version telemetry (`client_version_telemetry` capability), Rust SDK joins the compatibility maps (floor 0.7.0) |
| v9.5.0 - v9.6.1 | v8.0.0 | v8.5.x | OTLP `/v1/metrics` ingest (9.5.0); session-summary reporting API + Claude Code usage dashboard (9.6.0); audit-search `session_id` drill-down (9.6.1) |
| v9.2.0 - v9.4.0 | v8.0.0 | v8.5.x | Read-only MCP posture, tamper-evident audit signing, turnkey SIEM export (9.2.0); per-developer + per-session audit identity, audit read/report/export API, portal Log Explorer (9.3.0); capability-scoped detection (9.4.0) |
| v9.0.0 - v9.1.1 | v8.0.0 | v8.5.x | **Breaking:** canonical `policy_decision` vocabulary, DB-`CHECK`-enforced (`allow`→`allowed`, `deny`→`blocked`, `pending_approval`→`needs_approval`); `/decisions?decision=` rejects legacy filter values; compliance-export outcomes canonicalized (9.0.0). Audit coverage made permanent + sensitive-data lever enforced (9.1.0); container/CodeQL security patch (9.1.1) |
| v8.5.0 - v8.7.0 | v8.0.0 | v8.4.0-v8.5.x (Rust v0.6.0-0.7.0) | Decision Mode `context` propagation + Pasal 56(b) transfer basis + multi-arch images (8.5.0, SDK 8.4.0/Rust 0.6.0 train); request + response NIK/NPWP redaction on every plane, engine-fulfillable obligations (8.6.0); audit-trail consolidation (`decision_id`, `plane`, `correlation_id` on `audit_logs`) + per-org detection posture on every plane (8.7.0). SDK 8.5.0 / Rust 0.7.0 added the Decision Mode `decide → fulfill → forward` PEP contract |
| v8.2.0 - v8.4.0 | v8.0.0 | v8.2.0-v8.3.0 | Decision Mode PDP/PEP service (`POST /api/v1/decide`) + OTel decision tracer (8.2.0); `pii-indonesia` detection category, OJK/UU PDP compliance modules, OTel exporter configs - SDKs v8.3.0 / Rust v0.5.0 (8.3.0); OpenAI-compatible gateway endpoint (8.4.0) |
| v8.0.0 - v8.1.0 | v8.0.0 train (v7.x-era SDKs continue to work over HTTP) | v8.0.0 | **Breaking for direct SQL:** identity model separates `org_id` (customer org, RLS boundary), `client_id` (API credential), and deployment license identity; Row-Level Security enforced by default (FORCE RLS + non-owner `axonflow_app_role`); `X-Tenant-ID` accepted as deprecated alias; Go module path `/v8` (8.0.0). HITL outbound webhook callback + HTTP `Idempotency-Key` dedup (8.1.0) |
| v7.5.0 - v7.9.0 | v5.0.0 (Go/TS/Java), v6.0.0 (Python) | Go/TS v5.6.x, Py v6.6.x, Java v5.7.x | Per-category enforcement controls (7.6.0); Plugin Pro launches + structured upgrade envelope + Rust SDK announced (7.7.0/7.8.0); Decision History API + `policy_version` on every decision (7.9.0) |
| v7.3.0 - v7.4.5 | v5.0.0 (Go/TS/Java), v6.0.0 (Python) | Go/TS v5.6.0, Py v6.6.0, Java v5.7.0 | `retry_context` on every step-gate response + caller-supplied `idempotency_key` (7.3.0, ADR-045); HITL response parity across WCP/MAP + `/api/v1/plans/approvals/pending` (7.4.0, ADR-046) |
| v7.0.0 - v7.2.1 | v5.0.0 (Go/TS/Java), v6.0.0 (Python) | v5.x / v6.x | **Breaking:** default detection actions relaxed (PII default `redact`→`warn`); governance profiles `AXONFLOW_PROFILE` (`dev`/`default`/`strict`/`compliance`) + per-category `AXONFLOW_ENFORCE` (7.0.0); Community SaaS evaluation mode + self-registration (7.0.0); workflow checkpoints, idempotent step gates, governed overrides + explainability (7.1.0) |
| v6.1.0 | v5.0.0 (Go/TS/Java), v6.0.0 (Python) | v5.1.0 / v6.1.0 | Mistral LLM provider, Cursor/Codex integration, GovernedTool adapter (TS/Go/Java), checkToolInput/checkToolOutput aliases |
| v6.0.0 | v5.0.0 (Go/TS/Java), v6.0.0 (Python) | v5.1.0 / v6.1.0 | OAuth2 Basic auth required, legacy engine removed, agent single entry point, Go module v5 |
| v5.0.0 | v4.0.0 | v4.1.0 | Removed `total_steps` from create workflow, MCP operation default `"execute"`, Go module v4 |
| v4.8.0 | v3.8.0 | v3.8.0 | Version discovery, capability registry, User-Agent headers |
| v4.7.0 | v3.7.0 | v3.7.0 | MCP check-input/check-output endpoints, circuit breaker pipeline |
| v4.6.0 | v3.6.0 | v3.6.0 | Open-ended WCP workflows (optional total_steps) |
| v4.5.0 | v3.6.0 | v3.6.0 | WCP step-complete post-execution metrics |
| v4.4.0 | v3.5.0 | v3.5.0 | Media governance, cost controls |
| v4.3.0 | v3.4.0 | v3.4.0 | MAP, WCP, execution replay |
| v4.0.0 | v3.0.0 | v3.0.0 | Client credentials auth (OAuth2-style) |

Patch releases within a line (e.g. 9.14.1, 9.16.1, 9.2.1/9.2.2, 9.3.1, 8.5.1/8.5.2, 7.4.1-7.4.5) carry fixes only and keep their line's compatibility.

## Upgrade Guidance

- **Always upgrade the platform before or alongside SDK upgrades.** New SDK features may call endpoints that only exist in newer platform versions.
- The SDK 9.0.0 majors send the MCP server/tool identity as a separate `tool` wire field (no longer concatenated into `connector_type`). The platform accepts this on check-input from 9.10.0 and on check-output from 9.11.0; older platforms ignore the field.
- If you see a version mismatch warning in SDK logs, upgrade your platform to the recommended version.
- Use `healthCheckDetailed()` / `health_check_detailed()` to programmatically check what the platform supports before using new features.

## Backward Compatibility Guarantees

| Scenario | Behavior |
|----------|----------|
| **Old SDK + New Platform** | Safe for field ADDITIONS: old SDKs ignore unknown JSON fields. Exception: the v9.0.0 canonical-verdict cutover changes `policy_decision` values an integrator reads back - see the v8 → v9 migration guide. Exception: v10.0.0 changes what several read APIs put on the wire when they have nothing to report, and this is the same break set the v10.0.0 row above enumerates. In full: (1) `avg_latency_ms` becomes nullable on the audit report/summary and session-summary, and `tokens_used`, `cost` and `response_time_ms` are OMITTED rather than sent as `0` on audit search/export/get, so any client binding them to a non-optional number breaks; (2) the LEGACY `POST /api/v1/sebi/audit/export` gains `status: "partial"` and omits `summary.compliance_score` on an incomplete export, so an integration on that route keyed on `status === "completed"` breaks - note `POST /api/v1/compliance/reports` is a separate contract whose status vocabulary does NOT change and never returns `partial`; (3) declarative-workflow run ids from `POST /api/v1/workflows/execute` change prefix from `wf_` to `wfe_`, so **a client that prefix-matches `wf_` on those ids stops matching** - the ids stay opaque and nothing in the platform parses them, and ids minted before this release keep their old prefix; (4) `POST /api/v1/audit/report` now returns `500` where it previously returned a silently short top-policies table; (5) a `/api/v1/unified/executions` list carrying neither a tenant nor an org key is now `401` rather than an unscoped `200`; (6) on the MCP-server `check_policy` / `check_output` tools, an organisation holding an enabled segment-scoped policy must now send `X-User-Token` or the call is refused. |
| **New SDK + Old Platform** | Safe. New fields (`capabilities`, `sdk_compatibility`, `tool`) will be absent or ignored. SDKs return empty/nil values. |
| **Old SDK + Old Platform** | No change in behavior. |

## Capability Discovery

Starting with platform v4.8.0, the `/health` endpoint returns a `capabilities` array listing all supported features, plus `min_sdk_version` / `recommended_sdk_version` maps. SDKs can use this for runtime feature detection:

```go
// Go
health, _ := client.HealthCheckDetailed()
if health.HasCapability("mcp_check_endpoints") {
    // Safe to use MCP check endpoints
}
```

```python
# Python
health = client.health_check_detailed()
if health.has_capability("mcp_check_endpoints"):
    # Safe to use MCP check endpoints
    ...
```

```typescript
// TypeScript
const health = await client.healthCheck();
if (AxonFlow.hasCapability(health, 'mcp_check_endpoints')) {
    // Safe to use MCP check endpoints
}
```

```java
// Java
HealthStatus health = client.healthCheck();
if (health.hasCapability("mcp_check_endpoints")) {
    // Safe to use MCP check endpoints
}
```

Capabilities added in recent releases include `client_version_telemetry` (9.7.0), `identity_header_attribution` (9.9.0), and `seam_capability_decisioning` (9.11.0).
